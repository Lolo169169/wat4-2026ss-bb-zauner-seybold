{
  "name": "conduit-realworld-example-app",
  "version": "1.0.0",
  "description": "RealWorld example app, created to demonstrate a fully fledged fullstack application built with React / Express.js / Sequelize / PostgreSQL including CRUD operations, authentication, routing, pagination, and more.",
  "homepage": "https://github.com/TonyMckes/conduit-realworld-example-app#readme",
  "bugs": {
    "url": "https://github.com/TonyMckes/conduit-realworld-example-app/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/TonyMckes/conduit-realworld-example-app.git"
  },
  "license": "MIT",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently --names 'Node,Vite' -c 'green,blue' 'npm run dev -w backend' 'npm run dev -w frontend'",
    "sqlz": "npx -w backend sequelize-cli",
    "start": "npm run build -w frontend && npm run start -w backend",
    "db:up": "docker compose up -d db",
    "db:down": "docker compose down",
    "test": "npm run test:unit && npm run test:integration",
    "test:vitest": "vitest run",
    "test:vitest:coverage": "vitest run --coverage",
    "test:unit": "cross-env NODE_ENV=test jest tests/unit",
    "test:integration": "cross-env NODE_ENV=test jest tests/integration --runInBand",
    "test:jest": "cross-env NODE_ENV=test jest --runInBand",
    "test:jest:coverage": "cross-env NODE_ENV=test jest --runInBand --coverage",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:load": "k6 run --out json=tests/load/results/articles-load.json tests/load/articles-load.js && k6 run --out json=tests/load/results/auth-stress.json tests/load/auth-stress.js",
    "test:load:report": "node tests/load/analyze.js",
    "test:load:all": "npm run test:load && npm run test:load:report",
    "load:backend": "cross-env NODE_ENV=test npm run start -w backend",
    "test:load:local": "docker compose up -d --wait db && start-server-and-test load:backend http://127.0.0.1:3001 test:load:all"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "@vitejs/plugin-react-swc": "^4.2.3",
    "@vitest/coverage-v8": "^4.1.9",
    "concurrently": "^9.2.1",
    "cross-env": "^7.0.3",
    "jsdom": "^25.0.1",
    "start-server-and-test": "^3.0.11",
    "vitest": "^4.0.18"
  }
}
